# API KEY
API_KEY=gpt-db_api_key

# Optional port for local development
PORT=8000

# MongoDB via full URI (recommended):
# Paste the Atlas URI below, e.g.:
# mongodb+srv://<user>:<pass>@<cluster>/?retryWrites=true&w=majority
MONGO_URI=

# Or provide parts; the app will build the URI (password is URL-encoded):
# MONGO_HOST is typically something like: clustername.xxxxx.mongodb.net
MONGO_HOST=
MONGO_USER=
MONGO_PASS=
