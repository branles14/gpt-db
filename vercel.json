{
  "builds": [
    {
      "src": "app/app.py",
      "use": "@vercel/python",
      "config": {
        "entrypoint": "app.app:app"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "app/app.py" }
  ]
}
